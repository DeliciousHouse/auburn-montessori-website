---
import Layout from '../layouts/Layout.astro';
import resources from '../data/resources.json';
---

<Layout
  title="Parent Resources"
  description="Parent handbook, downloads, and helpful links for Auburn Montessori School - The Children's House."
>
  <header class="space-y-3">
    <h1 class="text-3xl font-extrabold tracking-tight text-slate-900">Parent Resources</h1>
    <p class="max-w-4xl text-base leading-relaxed text-slate-700">
      The Parent Handbook is the primary reference for detailed policies and procedures. Below are key documents (view or download), plus links to registration and other resources.
    </p>
  </header>

  <section class="mt-10 space-y-10">
    <h2 class="text-xl font-bold text-slate-900">Key Documents</h2>
    <p class="text-sm text-slate-600">
      View in the browser or use the download link if the viewer does not load.
    </p>
    {resources.keyDocuments.map((doc) => (
      <div class="card p-6">
        <h3 class="text-lg font-bold text-slate-900">{doc.label} (PDF)</h3>
        <p class="mt-2 text-sm text-slate-700">
          <a class="font-semibold no-underline text-blue-800 hover:text-blue-900" href={doc.href} download>
            Download {doc.label} (PDF)
          </a>
        </p>
        <div class="mt-4 overflow-hidden rounded-lg border border-slate-200">
          <iframe
            title={`${doc.label} PDF viewer`}
            src={`${doc.href}#view=FitH`}
            class="h-[70vh] w-full"
            loading="lazy"
          ></iframe>
        </div>
        <p class="mt-3 text-xs text-slate-600">
          If the PDF viewer does not load, use the download link above.
        </p>
      </div>
    ))}
  </section>

  <section class="mt-12 card p-6">
    <h2 class="text-lg font-bold text-slate-900">External services</h2>
    <p class="mt-2 text-sm text-slate-600">
      These links open in a new tab. Registration and some forms are hosted by third-party services (Brightwheel, Hubbli).
    </p>
    <ul class="mt-4 space-y-2 text-sm text-slate-700">
      {resources.externalServices.map((item) => (
        <li>
          <a
            class="font-semibold no-underline text-blue-800 hover:text-blue-900"
            href={item.href}
            target="_blank"
            rel="noopener noreferrer"
          >
            {item.label} (opens in a new tab)
          </a>
        </li>
      ))}
    </ul>
  </section>

  <section class="mt-8 card p-6">
    <h2 class="text-lg font-bold text-slate-900">Lists and learning</h2>
    <ul class="mt-4 space-y-2 text-sm text-slate-700">
      {resources.listsAndLearning.map((item) => (
        <li>
          <a
            class="font-semibold no-underline text-blue-800 hover:text-blue-900"
            href={item.href}
            target="_blank"
            rel="noopener noreferrer"
          >
            {item.label} (opens in a new tab)
          </a>
        </li>
      ))}
    </ul>
  </section>
</Layout>
