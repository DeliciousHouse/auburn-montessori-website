---
import Layout from '../layouts/Layout.astro';
import site from '../data/site.json';
import legal from '../data/legal.json';

const sections = [
  { id: 'features', label: 'Accessibility Features' },
  { id: 'pdfs', label: 'PDF Documents' },
  { id: 'feedback', label: 'Feedback' }
];
---

<Layout
  title="Accessibility Statement"
  description="Accessibility statement for the Auburn Montessori School - The Children's House website."
  heroTitle="Accessibility Statement"
  heroLead="Our commitment to an accessible website for all visitors."
>
  <div class="section-band">
    <div class="container-page flex gap-12">
      <aside class="hidden shrink-0 lg:block lg:w-48">
        <nav class="mini-nav" aria-label="On this page">
          <div class="mb-2 text-xs font-semibold uppercase tracking-wider" style="color: var(--color-neutral-500);">On this page</div>
          {sections.map((s) => (
            <a href={`#${s.id}`}>{s.label}</a>
          ))}
        </nav>
      </aside>

      <div class="legal-layout flex-1 text-sm leading-relaxed" style="color: var(--color-neutral-700);">
        <div class="badge mb-4">Last updated {legal.lastUpdated}</div>

        <p>
          {site.shortName} is committed to providing a website that is accessible to the widest possible audience,
          regardless of technology or ability.
        </p>

        <h2 id="features">Accessibility Features</h2>
        <p>
          This website uses semantic HTML, sufficient color contrast (4.5:1 minimum for body text), keyboard
          navigation support, and visible focus indicators. Pages are designed mobile-first and responsive.
          Links in body text are underlined for easy identification.
        </p>

        <h2 id="pdfs">PDF Documents</h2>
        <p>
          Some resources are provided as PDFs (such as the Parent Handbook). If you have difficulty accessing a
          document, please contact us and we will work with you to provide the information in an alternative format
          when possible.
        </p>

        <h2 id="feedback">Feedback</h2>
        <p>
          If you encounter an accessibility barrier on this site, please email <a class="font-semibold" href={`mailto:${site.email}`}>{site.email}</a> and include the page URL and a brief description of the issue.
        </p>
      </div>
    </div>
  </div>
</Layout>
