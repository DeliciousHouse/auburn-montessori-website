# Program photos

This folder stores local program marketing photos used by `/programs`.

- `preschool/` for Preschool images
- `elementary/` for Elementary (K–3rd Grade) images

Replace placeholder files with approved photos and keep file names the same, or update `src/data/programs.json`.
